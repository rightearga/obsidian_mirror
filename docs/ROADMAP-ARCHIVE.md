# Obsidian Mirror 路线图归档（v0.7.1 – v1.4.x）

> 本文档归档 v1.5.0 之前的所有版本规划记录，供历史查阅。  
> 当前路线图请见 [ROADMAP.md](./ROADMAP.md)。

---

## 🎉 v1.4.10 已实现功能摘要

截至 v1.3.0，Obsidian Mirror 已经实现以下核心功能：

#### 📝 内容处理
- ✅ Markdown 渲染（基于 pulldown-cmark）
- ✅ WikiLinks 支持 `[[笔记]]` 和 `[[笔记|别名]]`
- ✅ 图片和附件支持
- ✅ Frontmatter 元数据解析
- ✅ 代码高亮显示

#### 🔍 搜索与索引
- ✅ 全文搜索（Tantivy 引擎）
- ✅ 中文分词支持（jieba-rs）
- ✅ 搜索结果高亮
- ✅ 搜索历史记录
- ✅ 快捷键支持（Ctrl+K / Cmd+K）
- ✅ 高级搜索过滤（标签、文件夹、日期范围）
- ✅ 搜索结果排序（相关度/修改时间）

#### 🏷️ 标签系统
- ✅ Frontmatter 标签提取
- ✅ 内联标签 `#标签` 识别
- ✅ 标签云可视化
- ✅ 按标签过滤笔记

#### 🗺️ 导航与可视化
- ✅ 侧边栏文件树导航
- ✅ 反向链接显示
- ✅ 面包屑导航
- ✅ 笔记目录（TOC）自动生成
- ✅ 关系图谱（Vis.js，支持 1-3 层深度）

#### 🎨 用户体验
- ✅ 响应式设计（移动端适配）
- ✅ 深色/浅色主题切换
- ✅ 多语言支持（中文/English）
- ✅ 滚动位置记忆
- ✅ 平滑滚动动画
- ✅ 键盘导航支持
- ✅ 最近访问笔记（本地存储）
- ✅ 收藏夹功能（星标收藏）
- ✅ 笔记统计面板（总数、标签、最近更新）
- ✅ 笔记预览（悬浮卡片，前 200 字预览）
- ✅ 设置面板（语言、字体、主题统一管理）
- ✅ 内容自适应布局（宽度自动适配）
- ✅ 移动端 TOC 右侧滑入侧边栏
- ✅ 桌面端 TOC 可收起/展开

#### ⚙️ 系统功能
- ✅ Git 自动同步
- ✅ 增量同步（Git diff 检测）
- ✅ 索引持久化（postcard + redb）
- ✅ 忽略模式配置
- ✅ 高性能并发处理（Actix-web）
- ✅ 后台异步索引构建
- ✅ 健康检查和指标暴露
- ✅ 日志管理和优雅关闭
- ✅ 分享链接生成（带过期时间）
- ✅ 阅读进度跟踪（自动保存和恢复）

---

## 版本详情

### ✅ v0.7.1 (已发布 - 2026-01-29)

**主题**: 用户体验优化

- ✅ 界面文本全面中文化
- ✅ 移动端和桌面端侧边栏滚动位置记忆
- ✅ 修复移动端侧边栏交互问题
- ✅ 修复侧边栏多行文本对齐和缩进问题

---

### ✅ v0.8.0 (已发布 - 2026-01-29)

**主题**: 搜索功能 & 标签支持

#### 核心功能

- ✅ **全文搜索**
  - 使用 Tantivy 搜索引擎实现高性能全文索引
  - 在侧边栏添加搜索框
  - 支持标题和内容搜索（中文分词支持）
  - 实时搜索结果预览（300ms 防抖）
  - 高亮显示搜索关键词
  - 快捷键支持 (Ctrl+K / Cmd+K)
  - 搜索历史记录
  - 搜索结果排序（相关度优先）

- ✅ **标签系统**
  - 从 Frontmatter 中提取标签
  - 支持 `#标签` 语法识别
  - 标签页面展示所有标签（`/tags`）
  - 点击标签查看相关笔记列表
  - 标签云可视化（按使用频率显示大小）

- ✅ **性能优化**
  - 搜索索引优化（Tantivy 引擎）
  - 大型笔记库（5000+ 文件）加载优化
  - 后台异步索引构建，不阻塞主线程
  - 批量提交优化（每 1000 条笔记）
  - 50MB 写入缓冲区

#### 技术改进

- ✅ 重构索引构建逻辑，提取为独立模块（`indexer.rs`）
- ✅ 模块化代码结构（`handlers.rs`, `search_engine.rs`, `tags.rs`）

---

### ✅ v0.9.0 (已发布 - 2026-01-29)

**主题**: 可视化增强

#### 核心功能

- ✅ **笔记目录 (TOC)**
  - 自动生成页内目录（从 H1-H6 标题）
  - 点击跳转到对应章节（平滑滚动）
  - 浮动 TOC 支持（右侧固定）
  - 移动端收起/展开（折叠按钮）
  - 滚动时自动高亮当前章节
  - 支持多级嵌套标题

- ✅ **关系图谱**
  - 使用 Vis.js 实现笔记关系可视化
  - 显示当前笔记的直接和间接链接
  - 交互式节点（点击跳转、拖拽移动）
  - 可配置的图谱范围（1-3 层深度选择）
  - 物理引擎动画效果
  - 当前笔记节点高亮显示
  - 箭头表示链接方向

- ✅ **面包屑导航**
  - 显示当前笔记的路径层级
  - 点击跳转到上级路径
  - 移动端友好显示（紧凑布局）
  - 路径分隔符美化

#### 次要改进

- ✅ 笔记统计信息（字数、修改时间显示）
- ✅ 前端代码模块化（独立 JS 文件）
- ✅ CSS 模块化（按功能拆分样式文件）

#### 技术准备

- ✅ 模块化 JavaScript 代码（`static/js/` 目录）
- ✅ 模块化 CSS 代码（`static/css/` 目录）
- ✅ 引入 Vis.js 图谱库

---

### ✅ v0.10.0 (已发布 - 2026-01-29)

**主题**: 关系图谱完善

- ✅ 完善关系图谱交互体验
- ✅ 优化图谱性能和渲染效果
- ✅ 修复边缘情况和错误处理

---

### ✅ v1.0.0 (已发布 - 2026-01-31) 🎉

**主题**: 生产就绪

#### 核心功能

- ✅ **增量同步优化**
  - Git diff 检测变更文件
  - 仅处理修改/新增/删除的文件
  - 智能同步模式（首次克隆/增量更新/无变更）
  - 性能提升 10-100 倍

- ✅ **索引持久化**
  - 使用 postcard + redb 持久化笔记索引
  - Git 提交严格校验
  - 版本兼容性检查
  - 重启恢复速度提升 30-120 倍
  - 修复 Frontmatter 序列化问题

- ✅ **用户认证**
  - JWT 令牌认证
  - bcrypt 密码加密
  - 修改密码功能
  - 用户菜单界面
  - 登录/登出页面

#### 界面优化

- ✅ **侧边栏增强**
  - 默认宽度增加到 320px
  - 可拖动调整大小（200-600px）
  - 宽度记忆功能
  - 移除水平滚动条

- ✅ **滚动条样式统一**
  - 侧边栏、正文、目录滚动条统一样式
  - 使用 CSS 变量自动适配 Dark 主题

#### 运维功能

- ✅ **健康检查端点** (`/health`)
- ✅ **指标暴露** (Prometheus `/metrics`)
- ✅ **日志管理**：分级输出（`./logs/app.log`、`./logs/error.log`），每日轮转
- ✅ **优雅关闭处理**：捕获 SIGTERM/SIGINT，等待现有请求完成

#### 质量保证

- ✅ 37 个单元测试（Markdown 21、标签 8、认证 4、错误处理 2）
- ✅ 自定义 `AppError` 类型（8 种分类），统一 Result 类型

---

### ✅ v1.1.0 (已发布 - 2026-01-31) 🎉

**主题**: 功能增强

#### 核心功能

- ✅ **最近访问笔记**：最近 10 条，侧边栏快速访问，相对时间显示，折叠状态记忆
- ✅ **收藏夹功能**：星标图标，收藏列表管理，localStorage，Toast 提示
- ✅ **笔记统计面板**：笔记总数、标签数、近 7 天更新，3 个统计卡片

#### Bug 修复

- ✅ 标签持久化修复（重启后标签数据正确保留）

---

### ✅ v1.2.0 (已发布 - 2026-01-31) 🎉

**主题**: 体验优化

#### 核心功能

- ✅ **多语言支持**：中文/English 切换，语言偏好 localStorage 持久化
- ✅ **笔记预览**：悬浮卡片预览链接内容，前 200 字，平滑淡入淡出
- ✅ **设置面板**：语言、字体（系统/衬线/等宽）、字体大小（12-24px）、行高（1.2-2.0）
- ✅ **移动端 TOC 右侧滑入**：44×44px 切换按钮，0.3s ease 动画，85vw 自适应
- ✅ **桌面端 TOC 收起/展开**：展开状态 localStorage 记忆，平滑动画
- ✅ **内容自适应布局**：移除 800px 固定宽度，响应式边距

#### Bug 修复

- ✅ 移动端 TOC 侧边栏不显示
- ✅ `ignore_patterns` 配置变更不生效
- ✅ 表格溢出导致布局错位

---

### ✅ v1.3.0 (已发布 - 2026-02-01) 🎉

**主题**: 高级搜索功能

#### 核心功能

- ✅ **高级搜索过滤**：标签（多值 OR）、文件夹路径、日期范围，可折叠面板，一键清除
- ✅ **搜索结果排序**：按相关度（默认）或最新修改时间
- ✅ **分享链接生成**：自定义过期时间（1小时-30天），访问统计，redb 持久化
- ✅ **阅读进度跟踪**：实时保存滚动位置，刷新自动恢复，历史记录，redb 持久化

#### 后端改进

- ✅ 扩展搜索引擎支持标签和文件夹字段
- ✅ `advanced_search()` + BooleanQuery + RangeQuery
- ✅ 统一数据库配置到 `config.database`

---

### ✅ v1.3.1 (已发布 - 2026-04-13)

**主题**: Bug 修复与安全加固（CODEREVIEW_1.3 P0/P1）

- ✅ **[B1]** `persistence::clear()` 补充清空标签索引
- ✅ **[B4]** `Note` 添加 `outgoing_links` 字段，`BacklinkBuilder` 改为全量重建；`CURRENT_VERSION` 升至 2
- ✅ **[S1]** 标题文本 XSS 修复：`html_escape()` 转义 `&`, `<`, `>`, `"`
- ✅ **[B2]** `/sync` 并发保护：`AppState` 添加 `sync_lock`，并发时返回 409

**测试结果**: 52/52 通过，新增 5 个测试

---

### ✅ v1.3.2 (已发布 - 2026-04-13)

**主题**: 安全加固（CODEREVIEW_1.3 S2/S3/S4）

- ✅ **[S2]** Cookie 添加 `.secure(true).same_site(SameSite::Lax)`
- ✅ **[S3]** 分享密码改为 bcrypt 哈希存储（`password_hash` 字段）
- ✅ **[S4]** 认证中间件 `/login`、`/api/auth/login` 改为精确匹配

**测试结果**: 52/52 通过

---

### ✅ v1.3.3 (已发布 - 2026-04-13)

**主题**: 性能深度优化（CODEREVIEW_1.3 P1–P5）

- ✅ **[P1]** Regex 编译移至 `lazy_static!`（`markdown.rs` 5个、`tags.rs` 1个）
- ✅ **[P2]** Tantivy `IndexReader` 持久持有，搜索复用缓存，无需每次初始化
- ✅ **[P3]** 搜索索引增量更新：新增 `update_documents()`，增量同步只更新变更文件
- ✅ **[P4]** `reading_progress_db` 改用 redb `range()` 前缀查询，消除全表扫描
- ✅ **[P5]** `graph.rs` 改用 `note.outgoing_links`，消除热路径正则解析

**测试结果**: 52/52 通过

---

### ✅ v1.3.4 (已发布 - 2026-04-13)

**主题**: 代码质量改进 + 测试覆盖补全（CODEREVIEW_1.3 Q1–Q7）

- ✅ **[Q1]** `schema_matches` 同时检查字段名和字段类型变体，防止 schema 变更时复用错误索引
- ✅ **[Q3]** 阅读历史 `add_history()` 写入后自动调用 `cleanup_old_history(200)`
- ✅ **[Q5]** `truncate_html` 状态机去除 HTML 标签后再截断，确保 500 字为真实可见内容
- ✅ **[Q6]** 持久化笔记按 1000 条分批提交事务；元数据最后写入（原子完成标记）
- ✅ **[Q7]** `init_metrics()` 改用 `let _ =` 静默忽略 AlreadyReg，防测试 panic

**新增测试（14 个）**: `sync.rs` 4、`graph.rs` 6、`persistence.rs` 4  
**测试结果**: 66/66 通过

---

### ✅ v1.4.0 (已发布 - 2026-04-13)

**主题**: 键盘操作 + 主题定制 + 动画细节

#### 键盘快捷键

- ✅ `j`/`k` 平滑滚动半屏；`g g`/`G` 顶/底部；`[`/`]` 前后笔记；`b` 返回历史
- ✅ `t` 切换 TOC；`g` 打开图谱（450ms 无第二下触发）；`?` 快捷键帮助面板
- ✅ 帮助面板分组展示（导航/功能），`Esc` 关闭，`prefers-reduced-motion` 无动画

#### 主题定制

- ✅ 预设配色方案：暖色 / 护眼 / 高对比度（叠加深/浅色基础）
- ✅ 自定义主色调（颜色选择器，影响链接/标签/高亮，localStorage 持久化）
- ✅ 代码块主题独立选择：Atom One Dark / Light / GitHub / Dracula / Monokai

#### 交互动画

- ✅ 页面切换淡入（150ms ease-out），动画可关闭，`prefers-reduced-motion` 自动禁用
- ✅ 侧边栏文件夹图标旋转弹性过渡；搜索结果错峰进入（stagger 20ms/条）

**新增文件**: `keyboard.js/css`、`themes.css`、`animations.css`  
**测试结果**: 66/66 通过（纯前端变更）

---

### ✅ v1.4.1 (已发布 - 2026-04-13)

**主题**: Obsidian 语法扩展支持

- ✅ **KaTeX 数学公式**：`$...$` 行内 / `$$...$$` 块级，CDN 渲染，降级显示原始 LaTeX
- ✅ **Callout 块**（`> [!TYPE] Title`）：20+ 类型，可折叠（`-`/`+`），深/浅色配色
- ✅ **高亮语法**：`==高亮文本==` → `<mark>`
- ✅ **图片灯箱**：全屏模态，`←`/`→` 切换，`Esc` 关闭，alt 文本 + 计数
- ✅ **图片懒加载**：`markdown.rs` 为所有 `<img>` 添加 `loading="lazy"`

**新增测试（4 个）**: math_block / math_inline / highlight_syntax / image_lazy_loading  
**测试结果**: 70/70 通过

---

### ✅ v1.4.2 (已发布 - 2026-04-13)

**主题**: 搜索体验升级 + 笔记发现机制

- ✅ **实时搜索自动补全**：`GET /api/titles` 前端缓存，`#` 触发标签补全，sessionStorage TTL 5min
- ✅ **搜索历史升级**：最大 20 条，单条 `×` 悬停删除
- ✅ **孤立笔记页面**（`GET /orphans`）：无出链且无入链的笔记列表
- ✅ **随机漫游**（`GET /random`）：重定向随机笔记，快捷键 `r`
- ✅ **最近更新页面**（`GET /recent`）：按 mtime 降序，`?days=` 参数

**测试结果**: 70/70 通过

---

### ✅ v1.4.3 (已发布 - 2026-04-13)

**主题**: 全库图谱 + 视觉分组 + 布局多样化

- ✅ **全局图谱**（`GET /api/graph/global`）：全库链接关系，>500 节点自动降采样
- ✅ **图谱筛选面板**：标签/文件夹筛选，隐藏孤立节点，重置
- ✅ **节点大小映射**：大小与入度成正比（10–35 范围）
- ✅ **节点颜色分组**：第一个标签决定颜色，12 色调色板，图例面板
- ✅ **布局切换**：力导向（默认）/ 层级布局
- ✅ **图谱内搜索**：定位高亮匹配节点，视口动画移动
- ✅ **图谱导出**：Canvas toDataURL 导出 PNG

**domain.rs** `GraphNode.tags` 字段新增；图谱模态从 `page.html` 移至 `layout.html`  
**测试结果**: 74/74 通过（新增 4 个 graph 测试）

---

### ✅ v1.4.4 (已发布 - 2026-04-13)

**主题**: PWA + 触屏手势 + 无障碍

- ✅ **Service Worker + Web App Manifest**：可安装为 App，静态资源缓存优先，离线降级页面
- ✅ **触屏手势**：左边缘右滑开侧边栏，左滑关；内容区边界滑动翻页
- ✅ **Skip Link**：`<a href="#main-content">` 键盘 Tab 聚焦时浮现
- ✅ **ARIA 标注**：`role="tree"` / `role="listbox"` + `aria-live="polite"`
- ✅ **无障碍 CSS**：全局 `:focus-visible`，高对比度媒体查询，`prefers-reduced-motion` 完善

**测试结果**: 74/74 通过（纯前端变更）

---

### ✅ v1.4.5 (已发布 - 2026-04-13)

**主题**: 自动同步 + Webhook + 配置增强

- ✅ **定时自动同步**：`sync_interval_minutes` 配置，Tokio 定时任务遵守 `sync_lock` 互斥
- ✅ **Webhook 触发同步**（`POST /webhook/sync`）：GitHub HMAC 签名 + GitLab 令牌验证
- ✅ **配置热重载**（`POST /api/config/reload`，需认证）
- ✅ **ignore_patterns Glob 语法**：`*`/`**`/`?`，5 个新测试
- ✅ **Prometheus 指标扩展**：`sync_duration_seconds` + `sync_last_timestamp_seconds`
- ✅ **`/health` 扩展**：新增 `git_commit`、`sync_status`、`last_sync_at`、`last_sync_duration_ms`

**测试结果**: 79/79 通过（+5 scanner glob 测试）

---

### ✅ v1.4.6 (已发布 - 2026-04-13)

**主题**: Cookie 动态 Secure + Webhook HMAC + 调试日志清理

- ✅ **Cookie Secure 动态判断**：新增 `force_https_cookie: bool`（默认 `false`），解决 HTTP 环境登录失效
- ✅ **Webhook HMAC-SHA256 真实实现**：`hmac 0.12` + `sha2 0.10`，常数时间比较防时序攻击
- ✅ **graph.js 调试日志清理**：删除数十条 `console.log`，仅保留 `console.error`

---

### ✅ v1.4.7 (已发布 - 2026-04-13)

**主题**: 重启不重建搜索索引 + share_db 前缀查询 + 搜索结果增强

- ✅ **重启跳过搜索索引重建**：持久化命中 + `NoChange` 时直接复用 Tantivy 磁盘索引
- ✅ **搜索结果显示路径/标签/修改时间**：`SearchResult` 新增 `tags` 字段，前端卡片补充元信息
- ✅ **share_db 前缀查询**：主键改为 `{creator}:{token}` + 反查表，消除全表扫描

---

### ✅ v1.4.8 (已发布 - 2026-04-13)

**主题**: clippy 零警告 + 核心模块测试补全

- ✅ **clippy 零 warning**：修复约 33 处警告（`collapsible_if`、`needless_borrows`、`map_or` 等）
- ✅ **search_engine.rs 单元测试**（7 个）：基本搜索、标签/文件夹/日期过滤、num_docs、schema_matches
- ✅ **handlers.rs 集成测试**（3 个）：`/health`、`/api/titles`、`/orphans`

---

### ✅ v1.4.9 (已发布 - 2026-04-13)

**主题**: 移除 `Note.content_text`，内存占用减半

- ✅ **`Note.content_text` 完整移除**：CURRENT_VERSION 升至 3，强制缓存重建
- ✅ **同步管道重构**：`ProcessedNote` 增加 `Option<String>` content；content 在处理期传给 Tantivy 后丢弃，不存入 Note
- ✅ **持久化命中但 Tantivy 为空**：友好警告提示用户手动触发 `/sync`

---

### ✅ v1.4.10 (已发布 - 2026-04-13)

**主题**: 代码审计修复（CODEREVIEW_1.4）

- ✅ **[S1] assets_handler 路径遍历 P0**：`is_path_within` + `canonicalize` 前缀验证
- ✅ **[B1] sync_status FAILED**：RAII 守卫，异常退出自动设为 FAILED
- ✅ **[Q1] API 认证返回 401 JSON**：`/api/*` 路径返回 `401 + JSON`，页面路径重定向 `/login`
- ✅ **[Q4] git.rs 韩文注释**：替换为中文
- ✅ **8 个新测试**：is_path_within、assets 路径遍历回归、webhook HMAC-SHA256 签名验证

**测试结果**: 97/97 通过

---

## 📦 已完成功能分类（历史参考）

### 🚨 v1.3.x 紧急修复（来自 CODEREVIEW_1.3）

全部已完成：

| 编号 | 问题 | 版本 |
|------|------|------|
| B1 | `persistence::clear()` 未清理标签索引 | v1.3.1 |
| B4 | 增量同步反向链接数据丢失 | v1.3.1 |
| S1 | 标题文本 XSS 漏洞 | v1.3.1 |
| B2 | `/sync` 并发保护缺失 | v1.3.1 |
| S2 | Cookie 未设置 Secure/SameSite | v1.3.2 |
| S3 | 分享密码明文存储 | v1.3.2 |
| S4 | 认证中间件路径匹配宽松 | v1.3.2 |
| P1–P5 | 性能问题（lazy_static/Reader 缓存/增量索引/range 查询/graph 优化） | v1.3.3 |
| Q1–Q7 | 代码质量（schema 检查/历史清理/截断/分批写入/metrics 注册） | v1.3.4 |

### 🚨 v1.4.x 审计修复（来自 CODEREVIEW_1.4）

| 编号 | 问题 | 版本 |
|------|------|------|
| S1 | assets_handler 路径遍历 P0 | v1.4.10 |
| B1 | sync_status 失败不更新 | v1.4.10 |
| Q1 | API 认证返回 302 重定向 | v1.4.10 |
| Q4 | git.rs 韩文注释 | v1.4.10 |
